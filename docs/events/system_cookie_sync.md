# Event: system.cookie.sync

## Summary
Interní nebo cross-domain synchronizace cookies

## Structure

```js
sdl.push({
  event: "system.cookie.sync",
  system: {
    cookie: {
      sync: {}
    }
  }
});
```

